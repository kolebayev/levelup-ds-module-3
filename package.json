{
  "name": "levelup-ds-module-3",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html --out-dir dist/ --no-source-maps --no-cache",
    "build": "parcel build index.html --out-dir build/ --no-source-maps --no-minify --no-cache"
  },
  "author": "Ilya Kolebayev",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.4.0",
    "eslint-config-htmlacademy": "^5.0.0",
    "htmlhint": "^1.0.0",
    "parcel-bundler": "^1.12.5",
    "sass": "^1.44.0",
    "stylelint": "^14.1.0",
    "stylelint-config-htmlacademy": "^0.1.11",
    "stylelint-config-standard-scss": "^3.0.0"
  }
}